plugins {
    id 'java'
}

group 'com.gmail.shawnpkeene@gmail.com'
version '1.0.0'

sourceCompatibility = 1.8
project.ext.SERVER_DIR='C:\\Users\\InstantLightning\\Desktop\\Coding Coaching\\server\\plugins'
repositories {
    mavenCentral()
    mavenLocal()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'
    implementation group: 'org.spigotmc', name:'spigot', version: '1.12.2-R0.1-SNAPSHOT', transitive: false
}
task copyToServer(dependsOn: 'jar', type: Copy) {
    from 'build/libs/'
    into "${SERVER_DIR}"
}